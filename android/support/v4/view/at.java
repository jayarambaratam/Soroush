package android.support.v4.view;

import android.view.ViewGroup.MarginLayoutParams;

interface at {
    int m2235a(MarginLayoutParams marginLayoutParams);

    int m2236b(MarginLayoutParams marginLayoutParams);
}
