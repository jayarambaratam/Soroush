package android.support.v4.widget;

import android.support.v4.view.cb;
import android.view.View;

class bw implements bv {
    bw() {
    }

    public void m3338a(SlidingPaneLayout slidingPaneLayout, View view) {
        cb.m2395a(slidingPaneLayout, view.getLeft(), view.getTop(), view.getRight(), view.getBottom());
    }
}
